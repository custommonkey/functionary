package functionary


//TODO can we use context function to inject line

private val api = new Api{}

export api.flatMock
export api.expects
export api.never1
export api.never2
export api.expectsAny
export api.all
export api.FlatMock

